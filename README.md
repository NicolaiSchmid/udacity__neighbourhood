## Neighbourhood map
Hey, this is a simple React project for a Udacity ND.

To start it in development mode install all dependencies `yarn install` and run `yarn start`. To build a production version with a ServiceWorker run `yarn build`.

Please enjoy!

#### Credits
This application makes heavy use of the Google Maps and Foursquare Api. Thanks for providing these services for free!